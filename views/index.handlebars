<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="title">Eat-Da-Burger!</h1>
            <br>
            <h1 class="sub-title">Burger Time!</h1>
        </div>
    </div>

    {{!-- error is somewhere in these handlebars, can't find out what's causing my code to break}}
    {{!-- <div class="col-md-6">
        <h2>Waiting to Be Devoured</h2>
        <ul>
            {{#each burgers}} {{#unless devoured}} {{/unless}} {{/each}}
        </ul>
    </div>

    <div class="col-md-6">
        <h2>These Burgers Have Been Devoured</h2>
        <ul>
            {{#each burgers}} {{#unless devoured}} {{/if}} {{/each}}
        </ul>
    
</div> --}}

<div class="col-md-12">
    <h2>Name Your Own Burger!</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="br">Burger Name:</label>
        </div>

        <div class="form-group">
            <button type="submit" class="center-block btn btn-default"> Submit<input type="radio" name="devoured" value="0" checked>
            </button>
        </div>
    </form>
</div>
</div>
</div>
